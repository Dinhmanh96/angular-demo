import { ConversationDirective } from './conversation.directive';

describe('ConversationDirective', () => {
  it('should create an instance', () => {
    const directive = new ConversationDirective();
    expect(directive).toBeTruthy();
  });
});
